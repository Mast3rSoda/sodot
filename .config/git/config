
[core]
    editor = nvim

[commit]
    template = ~/.config/git/.gitmessage.txt

[merge]
    tool = vimdiff2

# uh, keep the username and mail in home gitconfig,
# so we don't push it to git
[include]
    path = ~/.gitconfig
